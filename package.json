{
  "name": "eduquinn-phase-16",
  "version": "16.0.0",
  "private": true,
  "description": "EduQuinn Phase 16 launch-ready learning marketplace",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node server.js",
    "migrate": "node --env-file-if-exists=.env scripts/migrate.js",
    "seed:admin": "node --env-file-if-exists=.env scripts/seed-admin.js",
    "check:production": "node --env-file-if-exists=.env scripts/production-check.js",
    "backup:db": "node --env-file-if-exists=.env scripts/backup-db.js",
    "maintenance": "node --env-file-if-exists=.env scripts/maintenance.js",
    "check:launch": "node --env-file-if-exists=.env scripts/launch-check.js",
    "first-run": "node --env-file-if-exists=.env scripts/first-run.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "pg": "^8.12.0"
  }
}
