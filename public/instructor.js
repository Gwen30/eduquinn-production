const root=document.querySelector('#instructorApp'),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m])),money=c=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(c||0)/100);async function api(u,o={}){return EduQuinnAuth.api(u,o)}
function layout(body,active='dashboard'){root.innerHTML=`<div class="studio-shell"><aside class="studio-sidebar"><a class="brand studio-brand" href="/"><span class="brand-mark">EQ</span><span><strong>EduQuinn</strong><small>Instructor Studio</small></span></a><nav><a class="${active==='dashboard'?'active':''}" href="/instructor.html">${iconSvg('dashboard')}<span>Dashboard</span></a><a class="${active==='courses'?'active':''}" href="/instructor-courses.html">${iconSvg('book')}<span>Courses</span></a><a href="/media-library.html">${iconSvg('upload')}<span>Media & lessons</span></a><a href="/instructor-live.html">${iconSvg('calendar')}<span>Live classes</span></a><a href="/instructor-messages.html">${iconSvg('review')}<span>Communication</span></a><a href="/instructor-announcements.html">${iconSvg('megaphone')}<span>Announcements</span></a><a href="/instructor-payouts.html">${iconSvg('payout')}<span>Payouts</span></a></nav><div class="studio-help"><b>Instructor support</b><p>Get help with publishing, payments or your account.</p><a href="/support.html">Contact support</a></div></aside><main class="studio-main"><header class="studio-top"><button class="studio-menu" id="studioMenu">${iconSvg('menu')}</button><div class="studio-user"><a class="icon-button" href="/notifications.html">${iconSvg('bell')}</a><a class="btn btn-ghost" href="/">Marketplace</a><button class="btn btn-primary" data-logout>Log out</button></div></header>${body}</main></div>`;document.querySelector('#studioMenu')?.addEventListener('click',()=>document.querySelector('.studio-sidebar')?.classList.toggle('open'))}
async function dashboard(){layout(`<section class="studio-page"><div class="studio-pagehead"><div><p class="eyebrow">INSTRUCTOR STUDIO</p><h1>Dashboard</h1><p>Your live course and earnings information.</p></div><a class="btn btn-primary" href="/media-library.html">Create course content</a></div><div id="realDashboard"></div></section>`);try{const [c,e]=await Promise.all([api('/api/instructor/courses'),api('/api/instructor/earnings')]);const published=c.courses.filter(x=>x.status==='published').length;document.querySelector('#realDashboard').innerHTML=`<div class="metric-grid"><article><span>${iconSvg('book')}</span><div><small>Courses</small><strong>${c.courses.length}</strong></div></article><article><span>${iconSvg('check')}</span><div><small>Published</small><strong>${published}</strong></div></article><article><span>${iconSvg('money')}</span><div><small>Available earnings</small><strong>${money(e.summary.available)}</strong></div></article><article><span>${iconSvg('chart')}</span><div><small>Lifetime earnings</small><strong>${money(e.summary.lifetime)}</strong></div></article></div><section class="studio-panel"><h2>Your courses</h2>${courseRows(c.courses)}</section>`}catch(x){document.querySelector('#realDashboard').innerHTML=`<div class="empty-state"><p>${esc(x.message)}</p></div>`}}
function courseRows(cs){return cs.length?`<div class="admin-table">${cs.map(c=>`<div class="course-tr"><div><b>${esc(c.title)}</b><small>${esc(c.education_level||'')} · ${esc(c.category||'')}</small></div><strong>${money(c.price_cents)}</strong><em class="status ${esc(c.status)}">${esc(c.status.replaceAll('_',' '))}</em><a class="table-action" href="/media-library.html?course=${encodeURIComponent(c.id)}">Manage</a></div>`).join('')}</div>`:'<div class="empty-state"><h3>No courses yet</h3><p>Create your first course from Media & lessons.</p><a class="btn btn-primary" href="/media-library.html">Create course</a></div>'}
async function coursesPage(){layout(`<section class="studio-page"><div class="studio-pagehead"><div><p class="eyebrow">COURSE MANAGEMENT</p><h1>My Courses</h1><p>Courses stored in your EduQuinn account.</p></div><a class="btn btn-primary" href="/media-library.html">New course</a></div><section class="studio-panel" id="courseList"></section></section>`,'courses');try{const d=await api('/api/instructor/courses');document.querySelector('#courseList').innerHTML=courseRows(d.courses)}catch(e){document.querySelector('#courseList').innerHTML=`<p>${esc(e.message)}</p>`}}
function builder(){layout(`<section class="studio-page"><div class="studio-pagehead"><div><p class="eyebrow">COURSE CREATION</p><h1>Course Builder</h1><p>Course creation and curriculum management are handled in the Media & lessons workspace.</p></div></div><section class="studio-panel"><a class="btn btn-primary" href="/media-library.html">Open Media & lessons</a></section></section>`,'courses')}
async function analytics(){layout(`<section class="studio-page"><div class="studio-pagehead"><div><p class="eyebrow">PERFORMANCE</p><h1>Analytics</h1><p>Live earnings and course counts from your account.</p></div></div><div id="analyticsReal"></div></section>`);try{const [c,e]=await Promise.all([api('/api/instructor/courses'),api('/api/instructor/earnings')]);document.querySelector('#analyticsReal').innerHTML=`<div class="metric-grid"><article><span>${iconSvg('book')}</span><div><small>Total courses</small><strong>${c.courses.length}</strong></div></article><article><span>${iconSvg('money')}</span><div><small>Available earnings</small><strong>${money(e.summary.available)}</strong></div></article><article><span>${iconSvg('clock')}</span><div><small>Pending earnings</small><strong>${money(e.summary.pending)}</strong></div></article><article><span>${iconSvg('chart')}</span><div><small>Lifetime earnings</small><strong>${money(e.summary.lifetime)}</strong></div></article></div>`}catch(e){document.querySelector('#analyticsReal').innerHTML=`<p>${esc(e.message)}</p>`}}
function onboarding(){const me=EduQuinnAuth.session();layout(`<section class="studio-page"><div class="studio-pagehead"><div><p class="eyebrow">INSTRUCTOR PROFILE</p><h1>Your instructor account</h1><p>Your verified registration information is stored securely with your account.</p></div></div><section class="studio-panel form-panel"><label>Name<input value="${esc(me?.name||'')}" disabled></label><label>Email<input value="${esc(me?.email||'')}" disabled></label><p>For identity verification or profile changes, contact EduQuinn Support.</p><a class="btn btn-primary" href="/support.html">Contact support</a></section></section>`)}
({dashboard,courses:coursesPage,builder,analytics,onboarding}[document.body.dataset.instructorPage]||dashboard)();
