const crypto=require('crypto');const bcrypt=require('bcryptjs');const db=require('../db');
(async()=>{const email=(process.env.ADMIN_EMAIL||'').trim().toLowerCase(),password=process.env.ADMIN_PASSWORD||'',name=process.env.ADMIN_NAME||'EduQuinn Administrator';if(!email||password.length<12)throw new Error('Set ADMIN_EMAIL and ADMIN_PASSWORD (12+ characters).');await db.migrate();const hash=await bcrypt.hash(password,12);await db.query(`INSERT INTO users(id,name,email,password_hash,role,status,email_verified) VALUES($1,$2,$3,$4,'admin','active',true) ON CONFLICT(email) DO UPDATE SET name=EXCLUDED.name,password_hash=EXCLUDED.password_hash,role='admin',status='active',updated_at=now()`,[crypto.randomUUID(),name,email,hash]);console.log('Administrator account ready:',email);process.exit(0)})().catch(e=>{console.error(e.message);process.exit(1)});
